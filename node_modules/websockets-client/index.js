const listeners = {}

export default function makeWebsocketClient(url) {
  if (typeof window === undefined) {
    // return noop
  }

  ws.addEventListener('open', function() {

  })
  ws.addEventListener('message', function() {

  })
  ws.addEventListener('error', function() {

  })
  ws.addEventListener('close', function() {
    
  })

  const on = (action, callback) => {
    switch(action) {
      case 'open':
      case 'message':
      case 'error':
      case 'close':
        break;
      default:
        listeners[action] = callback
    }
  }

  return {
    makeWebsocketClient,
    on,
    get connection() {
      
    }
  }
}

